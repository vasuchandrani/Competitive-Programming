#include<bits/stdc++.h>
using namespace std;
#define ll long long 

int main () {
    int t;  cin>>t;
    while (t--) {
        int n, q;
        cin>>n>>q;

        for (int i=0; i<q; i++) {
            string s;   cin>>s;

            if (s == "->") {
                ll x, y;
                cin>>x>>y;
                
            }
            else {
                ll d;   cin>>d;

            }
        }
    }
}