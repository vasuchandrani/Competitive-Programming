#include<bits/stdc++.h>
#include<iostream>
using namespace std;

int main () {
    int t;  cin>>t;
    while (t--) {
        int n, k;
        cin>>n>>k;
        int opr = 0;
        for (int i=0;i<n;i++) {
            int a, b;
            cin>>a>>b;
            int mini = min(a,b);
            
        }
    }
}